# Local device builds need a URL reachable from the phone, for example http://192.168.1.25:8001.
# Production should use the public backend origin without /api; the app appends /api itself.
EXPO_PUBLIC_BACKEND_URL=https://ikomekservice.kz
EXPO_PUBLIC_CLERK_PUBLISHABLE_KEY=
